import styled from 'styled-components/native';

const Input = styled.TextInput<any>`
  padding: 10px;
  padding-top: 12px;
  padding-bottom: 12px;
  background-color: #eee;
  border-radius: 10px;
`;

export default {
  Input,
};
